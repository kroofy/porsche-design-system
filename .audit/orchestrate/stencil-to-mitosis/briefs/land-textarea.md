GOAL         Land p-textarea from the Mitosis Lit probe into packages/components as the real p-textarea tag. Pixel-diff 0 vs the stored Stencil textarea playground baseline.
SCOPE        May write: packages/components (textarea source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/Textarea.lite.tsx if tagName must become p-textarea, .audit/orchestrate/stencil-to-mitosis/reports/land-textarea.md, /opt/cursor/artifacts/mitosis_land_textarea_after.png. May not write: other TAG_NAME sources, packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-input-week just passed: playground p-input-week is Mitosis Lit, baseline byte-equal (62817). Repeat that isolated wiring for p-textarea only. Playground: http://localhost:3333/?components=textarea. Card [data-card="textarea"] — 5 hosts (rows/counter/max-length variants: default, success+message, error+message, disabled, read-only). Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_textarea_before.png or /opt/cursor/artifacts/stencil_textarea_before.png (55279 bytes). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from textarea.tsx and full-restart --dev so bootstrapLazy drops p-textarea. Nested icons stay <p-icon> (already Mitosis). If HTMLPTextareaElement disappears from components.d.ts, restore it via stub-side d.ts like land-input-search. Tall card: page.screenshot({ clip }). Recipe cssText/shadow/no-fragments/alias still apply. Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles textarea as p-textarea. Mitosis Lit emits @customElement("p-textarea") with useShadowDom. rg my-fragment on generated Textarea.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
