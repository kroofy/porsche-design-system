GOAL         Land p-pin-code from the Mitosis Lit probe into packages/components as the real p-pin-code tag. Pixel-diff 0 vs the stored Stencil pin-code playground baseline.
SCOPE        May write: packages/components (pin-code source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/PinCode.lite.tsx if tagName must become p-pin-code, .audit/orchestrate/stencil-to-mitosis/reports/land-pin-code.md, /opt/cursor/artifacts/mitosis_land_pin_code_after.png. May not write: other TAG_NAME sources, packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-scroller just passed: playground p-scroller is Mitosis Lit, baseline byte-equal (31123). Repeat that isolated wiring for p-pin-code only. Playground: http://localhost:3333/?components=pin-code. Card [data-card="pin-code"] — 5 hosts (default, loading=true, success+message, error+message, disabled=true). All label="Some label", default length 4. fieldset.root with Label, digit inputs, StateMessage, optional loading p-spinner. Nested p-icon / p-spinner already Mitosis. Playground hosts have no light-DOM children. --p-animation-duration: 0s (and transition) so the loading spinner does not drift. hideLabel m is 1000. Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_pin_code_before.png or /opt/cursor/artifacts/stencil_pin_code_before.png (42246 bytes). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from pin-code.tsx and full-restart --dev so bootstrapLazy drops p-pin-code. If HTMLPPinCodeElement disappears from components.d.ts, restore it via stub-side d.ts. Tall card: page.screenshot({ clip }). Recipe cssText/shadow/no-fragments/alias still apply. Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles pin-code as p-pin-code. Mitosis Lit emits @customElement("p-pin-code") with useShadowDom. rg my-fragment on generated PinCode.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
