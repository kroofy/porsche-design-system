GOAL         Land p-toast from the Mitosis Lit probe into packages/components as the real p-toast tag. Pixel-diff 0 vs the stored Stencil toast playground baseline.
SCOPE        May write: packages/components (toast source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/Toast.lite.tsx if tagName must become p-toast, .audit/orchestrate/stencil-to-mitosis/reports/land-toast.md, /opt/cursor/artifacts/mitosis_land_toast_after.png. May not write: other TAG_NAME sources (including toast-item), packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-table-cell just passed: playground p-table-cell is Mitosis Lit, baseline byte-equal (57465). Repeat that isolated wiring for p-toast only. Playground: http://localhost:3333/?components=toast. Card [data-card="toast"] is four already-landed Mitosis p-button hosts plus one empty p-toast. Compare the closed/empty card — do not open messages. Nested p-toast-item is absent on this crop and stays Stencil this unit. Copy light-DOM children (empty). :host is position:fixed with --_p-toast-a, inset, z-index 999999. s is 760 (toast inset jumps at 760). m is 1000. Strip Mitosis .root. connectedCallback sets role=status. addMessage is a public Method used by the card buttons; port it or keep toast-manager so the real p-toast host still accepts addMessage, but the accept crop is the empty closed host. --p-animation-duration: 0s and --p-transition-duration: 0s. Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_toast_before.png or /opt/cursor/artifacts/stencil_toast_before.png (30687 bytes, SHA f1d76c7bf007f4a2bb3935b1d0a4356c488e7a8428fee03f62089624883a4994). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from toast.tsx and full-restart --dev so bootstrapLazy drops p-toast. If HTMLPToastElement disappears from components.d.ts, restore it via stub-side d.ts. Do not declare optional hidden on that interface. Recipe cssText/shadow/no-fragments/alias still apply. Probe Toast.lite.tsx still uses tagName lit-toast and a .root wrap — land must emit p-toast and strip .root. Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles toast as p-toast. Mitosis Lit emits @customElement("p-toast") with useShadowDom. rg my-fragment on generated Toast.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true. Crop [data-card="toast"] closed/empty.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
