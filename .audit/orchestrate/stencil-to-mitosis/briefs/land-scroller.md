GOAL         Land p-scroller from the Mitosis Lit probe into packages/components as the real p-scroller tag. Pixel-diff 0 vs the stored Stencil scroller playground baseline.
SCOPE        May write: packages/components (scroller source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/Scroller.lite.tsx if tagName must become p-scroller, .audit/orchestrate/stencil-to-mitosis/reports/land-scroller.md, /opt/cursor/artifacts/mitosis_land_scroller_after.png. May not write: other TAG_NAME sources, packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-pagination just passed: playground p-pagination is Mitosis Lit, baseline byte-equal (13801). Repeat that isolated wiring for p-scroller only. Playground: http://localhost:3333/?components=scroller. Card [data-card="scroller"] — 5 hosts (default slotted p-tags; indicator-position=top; scrollbar+top; scrollbar+center; indicator-sticky + 600x400 pink box). Card is ~230px wide so hosts overflow and .next is visible. Copy light-DOM children. Nested p-tag already Mitosis. Indicators are CSS ::after masks, not p-icon. indicator-sticky attr is ignored (prop is sticky). --p-transition-duration: 0s to pause indicator fades. Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_scroller_before.png or /opt/cursor/artifacts/stencil_scroller_before.png (31123 bytes). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from scroller.tsx and full-restart --dev so bootstrapLazy drops p-scroller. If HTMLPScrollerElement disappears from components.d.ts, restore it via stub-side d.ts. Tall card: page.screenshot({ clip }). Recipe cssText/shadow/no-fragments/alias still apply. Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles scroller as p-scroller. Mitosis Lit emits @customElement("p-scroller") with useShadowDom. rg my-fragment on generated Scroller.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
