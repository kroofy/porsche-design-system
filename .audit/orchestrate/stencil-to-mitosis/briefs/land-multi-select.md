GOAL         Land p-multi-select from the Mitosis Lit probe into packages/components as the real p-multi-select tag. Pixel-diff 0 vs the stored Stencil multi-select playground baseline.
SCOPE        May write: packages/components (multi-select source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/MultiSelect.lite.tsx if tagName must become p-multi-select, .audit/orchestrate/stencil-to-mitosis/reports/land-multi-select.md, /opt/cursor/artifacts/mitosis_land_multi_select_after.png. May not write: other TAG_NAME sources (including multi-select-option), packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-optgroup just passed: playground p-optgroup is Mitosis Lit, baseline byte-equal (33197). Repeat that isolated wiring for p-multi-select only. Playground: http://localhost:3333/?components=multi-select. Card [data-card="multi-select"] — 4 hosts, all class=w-full label="Some label" name="some-name" (default, success+message, error+message, disabled=true). Each has one direct p-multi-select-option and two p-optgroup children. Compare the closed card (combobox aria-expanded=false, popover not :popover-open) unless a host is already open. Options stay Stencil this unit. Nested p-optgroup and p-icon are already Mitosis. Copy light-DOM children. throwIfParentIsNotOfKind / throwIfElementIsNotOfKind on option/optgroup reconnect is benign. Built-in filter p-input-search and reset p-button-pure live inside the hidden popover or only when a value is set; playground hosts have no value so both stay out of the closed Lit tree. --p-animation-duration: 0s and --p-transition-duration: 0s. hideLabel m is 1000. Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_multi_select_before.png or /opt/cursor/artifacts/stencil_multi_select_before.png (33197 bytes). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from multi-select.tsx and full-restart --dev so bootstrapLazy drops p-multi-select. If HTMLPMultiSelectElement disappears from components.d.ts, restore it via stub-side d.ts. Mitosis Lit does not emit delegatesFocus or formAssociated. Recipe cssText/shadow/no-fragments/alias still apply. Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles multi-select as p-multi-select. Mitosis Lit emits @customElement("p-multi-select") with useShadowDom. rg my-fragment on generated MultiSelect.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
