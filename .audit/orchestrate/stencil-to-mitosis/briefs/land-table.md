GOAL         Land p-table from the Mitosis Lit probe into packages/components as the real p-table tag. Pixel-diff 0 vs the stored Stencil table playground baseline.
SCOPE        May write: packages/components (table source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/Table.lite.tsx if tagName must become p-table, .audit/orchestrate/stencil-to-mitosis/reports/land-table.md, /opt/cursor/artifacts/mitosis_land_table_after.png. May not write: other TAG_NAME sources (including table-head / table-body / rows / cells), packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-popover just passed: playground p-popover is Mitosis Lit, baseline byte-equal (54855). Repeat that isolated wiring for p-table only. Playground: http://localhost:3333/?components=table. Card [data-card="table"] spans 2 columns — 2 hosts (caption property → aria-label on .table; slotted p-heading caption + scroll-indicator CSS variables). Nested p-table-head / p-table-head-row / p-table-head-cell / p-table-body / p-table-row / p-table-cell stay Stencil this unit. Nested p-scroller and p-heading already Mitosis. Copy light-DOM children and named caption slot. Strip Mitosis .root so slotted rows/cells keep table display. Nested p-scroller keeps scrollbar=true and forwards compact / sticky. Host CSS variables (--_p-table-*, --p-scroller-indicator-*) inherit into nested table parts. throwIfParentIsNotOfKind on reconnect is benign. --p-animation-duration: 0s and --p-transition-duration: 0s. size m is 1000. Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_table_before.png or /opt/cursor/artifacts/stencil_table_before.png (57465 bytes). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from table.tsx and full-restart --dev so bootstrapLazy drops p-table. If HTMLPTableElement disappears from components.d.ts, restore it via stub-side d.ts. Do not declare optional hidden on that interface. Recipe cssText/shadow/no-fragments/alias still apply. Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles table as p-table. Mitosis Lit emits @customElement("p-table") with useShadowDom. rg my-fragment on generated Table.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
