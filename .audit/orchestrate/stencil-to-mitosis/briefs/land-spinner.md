GOAL         Land p-spinner from the Mitosis Lit probe into packages/components as the real p-spinner tag. Pixel-diff 0 vs the stored Stencil spinner playground baseline.
SCOPE        May write: packages/components (spinner source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/Spinner.lite.tsx if tagName must become p-spinner, .audit/orchestrate/stencil-to-mitosis/reports/land-spinner.md, /opt/cursor/artifacts/mitosis_land_spinner_after.png. May not write: other TAG_NAME sources, packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-display just passed: playground p-display is Mitosis Lit, baseline byte-equal (30525). Repeat that isolated wiring for p-spinner only. Playground: http://localhost:3333/?components=spinner. Card [data-card="spinner"]. Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_spinner_before.png or /opt/cursor/artifacts/stencil_spinner_before.png (41461 bytes). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from spinner.tsx and full-restart --dev so bootstrapLazy drops p-spinner. If the card is taller than the viewport, use page.screenshot({ clip }). Spinner animates: freeze with --p-animation-duration: 0s (and related duration vars if the baseline used them). Do not edit the baseline PNG. Recipe cssText/shadow/no-fragments/alias still apply. You are the only writer.
ACCEPTANCE   Stencil no longer compiles spinner as p-spinner. Mitosis Lit emits @customElement("p-spinner") with useShadowDom. rg my-fragment on generated Spinner.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
