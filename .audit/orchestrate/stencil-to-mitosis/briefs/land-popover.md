GOAL         Land p-popover from the Mitosis Lit probe into packages/components as the real p-popover tag. Pixel-diff 0 vs the stored Stencil popover playground baseline.
SCOPE        May write: packages/components (popover source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/Popover.lite.tsx if tagName must become p-popover, .audit/orchestrate/stencil-to-mitosis/reports/land-popover.md, /opt/cursor/artifacts/mitosis_land_popover_after.png. May not write: other TAG_NAME sources, packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-link-tile-product just passed: playground p-link-tile-product is Mitosis Lit, baseline byte-equal (88764). Repeat that isolated wiring for p-popover only. Playground: http://localhost:3333/?components=popover. Card [data-card="popover"] — 12 hosts including 1 nested. Nested p-button-pure / p-button / p-text already Mitosis. Copy light-DOM children and named button slot. Default closed state except hosts that already have open="true". Native [popover=manual] on the top layer — wait for :popover-open and Floating UI left/top before crop. A parent that contains nested popover children re-shows after those children update, so it paints above later sibling panels (Stencil hydration order). Default info button when there is no slot="button". Description renders <p>; otherwise default slot. Unset open is uncontrolled and stays closed. open="true" is controlled and calls showPopover(). --p-animation-duration: 0s and --p-transition-duration: 0s. size m is 1000. Card is taller than 900; clip to viewport remainder with page.screenshot({ clip }). If an already-open host overflows the card, crop the union of card + open host. Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_popover_before.png or /opt/cursor/artifacts/stencil_popover_before.png (54855 bytes). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from popover.tsx and full-restart --dev so bootstrapLazy drops p-popover. If HTMLPPopoverElement disappears from components.d.ts, restore it via stub-side d.ts. Do not declare optional hidden on that interface. Recipe cssText/shadow/no-fragments/alias still apply. Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles popover as p-popover. Mitosis Lit emits @customElement("p-popover") with useShadowDom. rg my-fragment on generated Popover.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
