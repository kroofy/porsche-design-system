GOAL         Land p-tabs-bar from the Mitosis Lit probe into packages/components as the real p-tabs-bar tag. Pixel-diff 0 vs the stored Stencil tabs-bar playground baseline.
SCOPE        May write: packages/components (tabs-bar source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/TabsBar.lite.tsx if tagName must become p-tabs-bar, .audit/orchestrate/stencil-to-mitosis/reports/land-tabs-bar.md, /opt/cursor/artifacts/mitosis_land_tabs_bar_after.png. May not write: other TAG_NAME sources (including tabs / tabs-item), packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-multi-select-option just passed: playground p-multi-select-option is Mitosis Lit, baseline byte-equal (33197). Repeat that isolated wiring for p-tabs-bar only. Playground: http://localhost:3333/?components=tabs-bar. Card [data-card="tabs-bar"] spans 2 columns — 7 hosts: default 3 buttons, active-tab-index=1 + 3 links, canvas / surface / frosted rails, size=medium with out-of-range 99 and with index 0. display:contents wrap around nested p-scroller.scroller (already Mitosis) plus default slot of button/a tabs and a zero-width .bar. Nested p-icon already Mitosis. Copy light-DOM children. Sanitize active-tab-index against live tab count. Recenter the active tab like Stencil scrollTabIntoView, then recenter with behavior instant if replaceWith resets the scroller. --p-animation-duration: 0s, --p-transition-duration: 0s, --p-duration-md: 0s, --p-duration-sm: 0s so delayed background-size and JS bar motion do not race the shot. size m is 1000. Card may be taller than 900; clip to viewport remainder with page.screenshot({ clip }). Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_tabs_bar_before.png or /opt/cursor/artifacts/stencil_tabs_bar_before.png (85161 bytes). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from tabs-bar.tsx and full-restart --dev so bootstrapLazy drops p-tabs-bar. If HTMLPTabsBarElement disappears from components.d.ts, restore it via stub-side d.ts. Recipe cssText/shadow/no-fragments/alias still apply. Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles tabs-bar as p-tabs-bar. Mitosis Lit emits @customElement("p-tabs-bar") with useShadowDom. rg my-fragment on generated TabsBar.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
