GOAL         Land p-select from the Mitosis Lit probe into packages/components as the real p-select tag. Pixel-diff 0 vs the stored Stencil select playground baseline.
SCOPE        May write: packages/components (select source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/Select.lite.tsx if tagName must become p-select, .audit/orchestrate/stencil-to-mitosis/reports/land-select.md, /opt/cursor/artifacts/mitosis_land_select_after.png. May not write: other TAG_NAME sources (including select-option and optgroup), packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-radio-group-option just passed: playground p-radio-group-option is Mitosis Lit, baseline byte-equal (93075). Repeat that isolated wiring for p-select only. Playground: http://localhost:3333/?components=select. Card [data-card="select"] — 4 hosts, all class=w-full label="Some label" name="some-name" filter=true (default, success+message, error+message, disabled=true). Each has one direct p-select-option and two p-optgroup children (five options). Compare the closed card (combobox aria-expanded=false, popover not :popover-open) unless a host is already open. Options and optgroups stay Stencil this unit. Nested p-icon already Mitosis. Copy light-DOM children. throwIfElementIsNotOfKind on option reconnect is benign. --p-animation-duration: 0s and --p-transition-duration: 0s. hideLabel m is 1000. Filter p-input-search lives inside the hidden popover and is omitted from the closed Lit tree; still copy filter="true". Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_select_before.png or /opt/cursor/artifacts/stencil_select_before.png (33197 bytes). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from select.tsx and full-restart --dev so bootstrapLazy drops p-select. If HTMLPSelectElement disappears from components.d.ts, restore it via stub-side d.ts. Mitosis Lit does not emit delegatesFocus or formAssociated. Recipe cssText/shadow/no-fragments/alias still apply. Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles select as p-select. Mitosis Lit emits @customElement("p-select") with useShadowDom. rg my-fragment on generated Select.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
