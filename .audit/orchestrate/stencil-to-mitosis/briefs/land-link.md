GOAL         Land p-link from the Mitosis Lit probe into packages/components as the real p-link tag. Pixel-diff 0 vs the stored Stencil link playground baseline.
SCOPE        May write: packages/components (link source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/Link.lite.tsx if tagName must become p-link, .audit/orchestrate/stencil-to-mitosis/reports/land-link.md, /opt/cursor/artifacts/mitosis_land_link_after.png. May not write: other TAG_NAME sources, packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-link-pure just passed: playground p-link-pure is Mitosis Lit, baseline byte-equal (97024). Repeat that isolated wiring for p-link only. Playground: http://localhost:3333/?components=link. Card [data-card="link"]. Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_link_before.png or /opt/cursor/artifacts/stencil_link_before.png (34250 bytes). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from link.tsx and full-restart --dev so bootstrapLazy drops p-link. Nested icons stay <p-icon> (already Mitosis). Omit unset href (Lit writes href="undefined" if set). If the card is taller than the viewport, use page.screenshot({ clip }). Recipe cssText/shadow/no-fragments/alias still apply. Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles link as p-link. Mitosis Lit emits @customElement("p-link") with useShadowDom. rg my-fragment on generated Link.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
