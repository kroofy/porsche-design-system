GOAL         Land p-banner from the Mitosis Lit probe into packages/components as the real p-banner tag. Pixel-diff 0 vs the stored Stencil banner playground baseline.
SCOPE        May write: packages/components (banner source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/Banner.lite.tsx if tagName must become p-banner, .audit/orchestrate/stencil-to-mitosis/reports/land-banner.md, /opt/cursor/artifacts/mitosis_land_banner_after.png. May not write: other TAG_NAME sources, packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-inline-notification just passed: playground p-inline-notification is Mitosis Lit, baseline byte-equal (127470). Repeat that isolated wiring for p-banner only. Playground: http://localhost:3333/?components=banner. Card [data-card="banner"] — 5 hosts (first open="true" heading/description heading-tag=h3; four closed slotted heading+description, one dismiss-button=false, success/warning/error). Host is display:contents popover=manual; open banner sits top-layer at 1440 (position {base:bottom,s:top}, s=760 not m=1000). Crop union of card + open popover. Playwright wait attached, not visible. Nested p-heading/p-text/p-button already Mitosis. Copy light-DOM children + named slots. Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_banner_before.png or /opt/cursor/artifacts/stencil_banner_before.png (57293 bytes). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from banner.tsx and full-restart --dev so bootstrapLazy drops p-banner. If HTMLPBannerElement disappears from components.d.ts, restore it via stub-side d.ts. skipEntryTransition so the already-open host does not run @starting-style. Recipe cssText/shadow/no-fragments/alias still apply. Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles banner as p-banner. Mitosis Lit emits @customElement("p-banner") with useShadowDom. rg my-fragment on generated Banner.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. s is 760. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
