GOAL         Land p-text from the Mitosis Lit probe into packages/components as the real p-text tag. Pixel-diff 0 vs the stored Stencil text playground baseline.
SCOPE        May write: packages/components (text source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/Text.lite.tsx if tagName must become p-text, .audit/orchestrate/stencil-to-mitosis/reports/land-text.md, /opt/cursor/artifacts/mitosis_land_text_after.png. May not write: other TAG_NAME sources, packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-heading just passed: playground p-heading is Mitosis Lit, baseline byte-equal (55470). Repeat that isolated wiring for p-text only. Playground: http://localhost:3333/?components=text. Card [data-card="text"]. Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_text_before.png or /opt/cursor/artifacts/stencil_text_before.png (55035 bytes). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from text.tsx and full-restart --dev so bootstrapLazy drops p-text. If the card is taller than the viewport, use page.screenshot({ clip }), not locator.screenshot() of a clipped descendant. Recipe cssText/shadow/no-fragments/alias still apply. Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles text as p-text. Mitosis Lit emits @customElement("p-text") with useShadowDom. rg my-fragment on generated Text.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
