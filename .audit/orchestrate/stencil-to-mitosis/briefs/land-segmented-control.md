GOAL         Land p-segmented-control from the Mitosis Lit probe into packages/components as the real p-segmented-control tag. Pixel-diff 0 vs the stored Stencil segmented-control playground baseline.
SCOPE        May write: packages/components (segmented-control source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/SegmentedControl.lite.tsx if tagName must become p-segmented-control, .audit/orchestrate/stencil-to-mitosis/reports/land-segmented-control.md, /opt/cursor/artifacts/mitosis_land_segmented_control_after.png. May not write: other TAG_NAME sources (including segmented-control-item), packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-accordion just passed: playground p-accordion is Mitosis Lit, baseline byte-equal (70863). Repeat that isolated wiring for p-segmented-control only. Playground: http://localhost:3333/?components=segmented-control. Card [data-card="segmented-control"] — 3 hosts, all class=w-full, four items each (default, disabled=true, mixed item-disabled). fieldset.root with Label + default slot of p-segmented-control-item + StateMessage. Items stay Stencil this unit. Nested p-icon already Mitosis (car is in LitIcon map; like may need CDN source). Copy light-DOM children. hideLabel m is 1000. Measure item widths like Stencil getItemWidths. Omit empty label-wrapper. Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_segmented_control_before.png or /opt/cursor/artifacts/stencil_segmented_control_before.png (36882 bytes). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from segmented-control.tsx and full-restart --dev so bootstrapLazy drops p-segmented-control. If HTMLPSegmentedControlElement disappears from components.d.ts, restore it via stub-side d.ts. Recipe cssText/shadow/no-fragments/alias still apply. Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles segmented-control as p-segmented-control. Mitosis Lit emits @customElement("p-segmented-control") with useShadowDom. rg my-fragment on generated SegmentedControl.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
RESUME       Source is 23c19ec88e. After PNG exists but is not byte-equal (39903 vs stored 36882). Last pixel-diff resume aborted before any tools. Playground http://localhost:3333 is down — restart packages/components `npm start` (stencil --dev). Do not wait for cssText to include the literal "1000": this card has no hideLabel/columns breakpoint objects. measureItemWidths currently appends the temp node to shadowRoot during Lit render and returns NaN, which emits `repeat(auto-fit, NaNpx)` and a taller card. Measure on document.body (or after updateComplete), keep only finite widths, rebuild the IIFE, then recapture `/opt/cursor/artifacts/mitosis_land_segmented_control_after.png`. Do not land the item. Write the report only after pixel-diff 0.
