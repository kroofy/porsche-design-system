GOAL         Land p-optgroup from the Mitosis Lit probe into packages/components as the real p-optgroup tag. Pixel-diff 0 vs the stored Stencil optgroup playground baseline.
SCOPE        May write: packages/components (optgroup source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/Optgroup.lite.tsx if tagName must become p-optgroup, .audit/orchestrate/stencil-to-mitosis/reports/land-optgroup.md, /opt/cursor/artifacts/mitosis_land_optgroup_after.png. May not write: other TAG_NAME sources (including already-landed select and select-option), packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-select-option just passed: playground p-select-option is Mitosis Lit, baseline byte-equal (33197). Repeat that isolated wiring for p-optgroup only. Playground: http://localhost:3333/?components=select. There is no separate optgroup card — crop [data-card="select"] (4 closed parent hosts, 8 optgroups: label "Some optgroup", second group disabled=true, two options each). Compare the closed card. Parent is already Mitosis (LitSelect). Options are already Mitosis (LitSelectOption). Copy light-DOM children. throwIfParentIsNotOfKind on option reconnect is benign. Optgroups sit in a display:none popover — wait attached, not visible. --p-animation-duration: 0s and --p-transition-duration: 0s. Do not declare optional hidden on HTMLPOptgroupElement if that breaks native HTMLElement.hidden (see land-select-option host-type fix). Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_optgroup_before.png or /opt/cursor/artifacts/stencil_optgroup_before.png (33197 bytes, same crop as the select card). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from optgroup.tsx and full-restart --dev so bootstrapLazy drops p-optgroup. If HTMLPOptgroupElement disappears from components.d.ts, restore it via stub-side d.ts. Recipe cssText/shadow/no-fragments/alias still apply. Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles optgroup as p-optgroup. Mitosis Lit emits @customElement("p-optgroup") with useShadowDom. rg my-fragment on generated Optgroup.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
