GOAL         Land p-button-tile from the Mitosis Lit probe into packages/components as the real p-button-tile tag. Pixel-diff 0 vs the stored Stencil button-tile playground baseline.
SCOPE        May write: packages/components (button-tile source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/ButtonTile.lite.tsx if tagName must become p-button-tile, .audit/orchestrate/stencil-to-mitosis/reports/land-button-tile.md, /opt/cursor/artifacts/mitosis_land_button_tile_after.png. May not write: other TAG_NAME sources, packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-stepper-horizontal-item just passed: playground p-stepper-horizontal-item is Mitosis Lit, baseline byte-equal (56074). Repeat that isolated wiring for p-button-tile only. Playground: http://localhost:3333/?components=button-tile. Card [data-card="button-tile"] — 5 hosts (3/4 default; 9/16 top + gradient + header/footer; auto + gradient + header/footer; disabled; loading). Nested p-button / p-tag / p-text already Mitosis. Copy light-DOM children and named header / footer / default img slots. Always render both p-buttons (CSS hides the unused one). Restore .root / .media / .footer class names if Mitosis drops them. footer-slot hasFooterSlot for compact grid-row. compact JSON m is 1000. Mitosis Lit does not emit delegatesFocus — do not fake it unless the stored baseline already matches without it. --p-animation-duration: 0s and --p-transition-duration: 0s so the loading spinner and media hover scale do not race the shot. Card is taller than 900; clip to viewport remainder with page.screenshot({ clip }). Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_button_tile_before.png or /opt/cursor/artifacts/stencil_button_tile_before.png (397967 bytes). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from button-tile.tsx and full-restart --dev so bootstrapLazy drops p-button-tile. If HTMLPButtonTileElement disappears from components.d.ts, restore it via stub-side d.ts. Do not declare optional hidden on that interface. Recipe cssText/shadow/no-fragments/alias still apply. Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles button-tile as p-button-tile. Mitosis Lit emits @customElement("p-button-tile") with useShadowDom. rg my-fragment on generated ButtonTile.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
