GOAL         Land p-fieldset from the Mitosis Lit probe into packages/components as the real p-fieldset tag. Pixel-diff 0 vs the stored Stencil fieldset playground baseline.
SCOPE        May write: packages/components (fieldset source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/Fieldset.lite.tsx if tagName must become p-fieldset, .audit/orchestrate/stencil-to-mitosis/reports/land-fieldset.md, /opt/cursor/artifacts/mitosis_land_fieldset_after.png. May not write: other TAG_NAME sources, packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-textarea just passed: playground p-textarea is Mitosis Lit, baseline byte-equal (55279). Repeat that isolated wiring for p-fieldset only. Playground: http://localhost:3333/?components=fieldset. Card [data-card="fieldset"] — 4 hosts, each with two slotted p-input-text (already Mitosis): default, label-size=small, success+message, error+message. Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_fieldset_before.png or /opt/cursor/artifacts/stencil_fieldset_before.png (57315 bytes). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from fieldset.tsx and full-restart --dev so bootstrapLazy drops p-fieldset. Copy light-DOM children on swap. If HTMLPFieldsetElement disappears from components.d.ts, restore it via stub-side d.ts like land-input-search. Tall card: page.screenshot({ clip }). Recipe cssText/shadow/no-fragments/alias still apply. Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles fieldset as p-fieldset. Mitosis Lit emits @customElement("p-fieldset") with useShadowDom. rg my-fragment on generated Fieldset.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
