GOAL         Land p-modal from the Mitosis Lit probe into packages/components as the real p-modal tag. Pixel-diff 0 vs the stored Stencil modal playground baseline.
SCOPE        May write: packages/components (modal source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/Modal.lite.tsx if tagName must become p-modal, .audit/orchestrate/stencil-to-mitosis/reports/land-modal.md, /opt/cursor/artifacts/mitosis_land_modal_after.png. May not write: other TAG_NAME sources (including flyout), packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-toast-item just passed: playground p-toast-item is Mitosis Lit, union-crop baseline byte-equal (106421). Repeat that isolated wiring for p-modal only. Playground: http://localhost:3333/?components=modal. Card [data-card="modal"] has 5 closed p-modal hosts plus already-landed Mitosis p-button openers. Compare the default closed card — do not open dialogs. Nested p-text / p-heading / p-button are already Mitosis. Copy light-DOM children and named header / footer slots. :host is display:contents. Closed dialog collapses to width 0 / height 0 / visibility hidden. DialogBase is inlined (dialog > .scroller > .modal + native button.dismiss + slots). FCDismissButton is a native button + CSS mask, not p-button-pure. showModal only when open. fullscreen parses JSON attrs that start with {. Card is taller than 900; clip to viewport remainder with page.screenshot({ clip }). --p-animation-duration: 0s and --p-transition-duration: 0s. size m is 1000 (fullscreen media query). Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_modal_before.png or /opt/cursor/artifacts/stencil_modal_before.png (48122 bytes, SHA daa2e78d1785ad54b32a07611c2a433512d48dce4cebd09407ac9f125aa7c038). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from modal.tsx and full-restart --dev so bootstrapLazy drops p-modal. If HTMLPModalElement disappears from components.d.ts, restore it via stub-side d.ts. Do not declare optional hidden on that interface. Recipe cssText/shadow/no-fragments/alias still apply. Probe Modal.lite.tsx still uses tagName lit-modal — land must emit p-modal. Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles modal as p-modal. Mitosis Lit emits @customElement("p-modal") with useShadowDom. rg my-fragment on generated Modal.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true. Crop closed [data-card="modal"].
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
