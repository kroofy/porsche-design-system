GOAL         Land p-table-head from the Mitosis Lit probe into packages/components as the real p-table-head tag. Pixel-diff 0 vs the stored Stencil table-head playground baseline.
SCOPE        May write: packages/components (table-head source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/TableHead.lite.tsx if tagName must become p-table-head, .audit/orchestrate/stencil-to-mitosis/reports/land-table-head.md, /opt/cursor/artifacts/mitosis_land_table_head_after.png. May not write: other TAG_NAME sources (including table-head-row / table-head-cell / table-body), packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-table just passed: playground p-table is Mitosis Lit, baseline byte-equal (57465). Repeat that isolated wiring for p-table-head only. Playground: http://localhost:3333/?components=table. There is no separate table-head card — crop [data-card="table"] (2 parent tables, 2 heads). Parent is already Mitosis (LitTable) and supplies --_p-table-c. Nested p-table-head-row / p-table-head-cell stay Stencil this unit. Copy light-DOM children. :host is table-header-group. Strip Mitosis .root so slotted p-table-head-row stays a direct table-row child. connectedCallback sets role=rowgroup. Slotted --_p-table-d / --_p-table-b zero the row divider so the head border is the only line. throwIfParentIsNotOfKind on reconnect is benign. --p-animation-duration: 0s and --p-transition-duration: 0s. size m is 1000. Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_table_head_before.png or /opt/cursor/artifacts/stencil_table_head_before.png (57465 bytes, same crop SHA as the table card). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from table-head.tsx and full-restart --dev so bootstrapLazy drops p-table-head. If HTMLPTableHeadElement disappears from components.d.ts, restore it via stub-side d.ts. Do not declare optional hidden on that interface. Recipe cssText/shadow/no-fragments/alias still apply. Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles table-head as p-table-head. Mitosis Lit emits @customElement("p-table-head") with useShadowDom. rg my-fragment on generated TableHead.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
