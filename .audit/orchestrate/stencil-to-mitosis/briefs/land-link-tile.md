GOAL         Land p-link-tile from the Mitosis Lit probe into packages/components as the real p-link-tile tag. Pixel-diff 0 vs the stored Stencil link-tile playground baseline.
SCOPE        May write: packages/components (link-tile source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/LinkTile.lite.tsx if tagName must become p-link-tile, .audit/orchestrate/stencil-to-mitosis/reports/land-link-tile.md, /opt/cursor/artifacts/mitosis_land_link_tile_after.png. May not write: other TAG_NAME sources (including link-tile-product), packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-button-tile just passed: playground p-button-tile is Mitosis Lit, baseline byte-equal (397967). Repeat that isolated wiring for p-link-tile only. Playground: http://localhost:3333/?components=link-tile. Card [data-card="link-tile"] — 3 hosts (3/4 default; 9/16 top + gradient + header/footer; auto + gradient + header/footer). All have href and target=_blank. Nested p-link / p-tag / p-text already Mitosis. Copy light-DOM children and named header / footer / default img slots. Always render both p-links (CSS hides the unused one). Restore .root / .media / .footer class names if Mitosis drops them. Stretched overlay <a tabindex="-1" aria-hidden="true">. Unset href / download / rel must omit the attribute (Lit nothing is truthy; do not emit href="undefined"). Root has no cursor (unlike button-tile). footer-slot hasFooterSlot for compact grid-row. compact JSON m is 1000. Mitosis Lit does not emit delegatesFocus — do not fake it unless required for byte-equal. --p-animation-duration: 0s and --p-transition-duration: 0s so media hover scale does not race the shot. Card is taller than 900; clip to viewport remainder with page.screenshot({ clip }). Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_link_tile_before.png or /opt/cursor/artifacts/stencil_link_tile_before.png (397967 bytes). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from link-tile.tsx and full-restart --dev so bootstrapLazy drops p-link-tile. If HTMLPLinkTileElement disappears from components.d.ts, restore it via stub-side d.ts. Do not declare optional hidden on that interface. Recipe cssText/shadow/no-fragments/alias still apply. Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles link-tile as p-link-tile. Mitosis Lit emits @customElement("p-link-tile") with useShadowDom. rg my-fragment on generated LinkTile.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
