GOAL         Remove Stencil as the packages/components compiler so the playground no longer runs `stencil build --dev`. All 75 TAG_NAMES already serve Mitosis Lit. Prove it by pixel-diff 0 on stored divider and canvas baselines after the compiler is gone.
SCOPE        May write: packages/components (package.json start/scripts, stencil.config.ts, index.html loader, a non-Stencil static/dev serve path), .audit/orchestrate/stencil-to-mitosis/reports/delete-stencil.md, verify artifacts under /opt/cursor/artifacts/delete_stencil_*.png. May not write: new component .lite.tsx, packages/mitosis-probe/**, force-deleting every leftover stub/spec in one unproven sweep, framework wrapper regeneration.
CONTEXT      land-canvas just passed: playground p-canvas is Mitosis Lit, chrome crop byte-equal (40157). Every TAG_NAME host is a stub without a live @Component. Playground still starts via `PDS_IS_STAGING=1 stencil build --watch --serve --dev` on :3333. That is the remaining Stencil compiler. Replace it with a static/dev server that serves the existing Mitosis IIFEs + playground index.html on :3333. Do not recapture baselines. Re-run land-divider and land-canvas pixel-diff scripts (or equivalent) against stored baselines: divider .audit/orchestrate/stencil-to-mitosis/baseline/stencil_divider_before.png and canvas stencil_canvas_before.png (40157, SHA 28de5f4bfb3bfd7be7a26beebb89bb114b419f318af8c9e777e7dea52ff18c97). Both must stay byte-equal / strictMismatch 0. Viewport 1440x900 dsf 2. Keep IIFE script tags. Do not delete mitosis/ output or src/assets/p-*.iife.js. Do not start a second product change. You are the only writer.
ACCEPTANCE   `packages/components` start path no longer invokes `stencil build`. rg '^@Component' on packages/components/src/**/*.tsx is empty. Live divider card and canvas chrome pixel-diff 0 vs stored baselines. Report written.
VERIFY       Recipe pixel-diff on divider + canvas. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no re-landing a tag, no customElement/webcomponent targets, no deleting stored baselines.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. Prove it works before deleting more leftover specs.
