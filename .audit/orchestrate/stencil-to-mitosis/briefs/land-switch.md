GOAL         Land p-switch from the Mitosis Lit probe into packages/components as the real p-switch tag. Pixel-diff 0 vs the stored Stencil switch playground baseline.
SCOPE        May write: packages/components (switch source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/Switch.lite.tsx if tagName must become p-switch, .audit/orchestrate/stencil-to-mitosis/reports/land-switch.md, /opt/cursor/artifacts/mitosis_land_switch_after.png. May not write: other TAG_NAME sources, packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-button just passed: playground p-button is Mitosis Lit, baseline byte-equal (52720). Repeat that isolated wiring for p-switch only. Playground: http://localhost:3333/?components=switch. Card [data-card="switch"]. Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_switch_before.png or /opt/cursor/artifacts/stencil_switch_before.png (35256 bytes). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from switch.tsx and full-restart --dev so bootstrapLazy drops p-switch. If HTMLPSwitchElement disappears from components.d.ts, restore it like land-button-pure. Tall card: page.screenshot({ clip }). Recipe cssText/shadow/no-fragments/alias still apply. Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles switch as p-switch. Mitosis Lit emits @customElement("p-switch") with useShadowDom. rg my-fragment on generated Switch.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
