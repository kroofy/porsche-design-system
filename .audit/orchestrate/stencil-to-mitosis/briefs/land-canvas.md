GOAL         Land p-canvas from the Mitosis Lit probe into packages/components as the real p-canvas tag. Pixel-diff 0 vs the stored Stencil canvas playground chrome baseline.
SCOPE        May write: packages/components (canvas source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/Canvas.lite.tsx if tagName must become p-canvas, .audit/orchestrate/stencil-to-mitosis/reports/land-canvas.md, /opt/cursor/artifacts/mitosis_land_canvas_after.png. May not write: other TAG_NAME sources, packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-carousel just passed: playground p-carousel is Mitosis Lit, closed-card baseline byte-equal (7096). Repeat that isolated wiring for p-canvas only. There is no [data-card="canvas"]. p-canvas is the playground page shell. Playground: http://localhost:3333/?components=divider. One host. Both sidebars open: sidebar-start-open="true" sidebar-end-open="true". background="surface" is a property, not an attribute on the live host — copy the property on swap (do not rely on getAttribute). Nested shadow p-button / p-crest / p-wordmark are already-landed Mitosis. Copy light-DOM children and named slots title / header-start / header-end / footer / sidebar-start / sidebar-end / sidebar-end-header / background / default. Crop the union of shadow .header + .sidebar__header--start + .sidebar__header--end (2880×112 at dsf 2 / 1440×56 CSS). Do not crop scrolling main. :host is display:block. Stencil markup already has one .root — keep that single .root; do not add a second Mitosis wrapper. css branches on sidebarStartOpen, sidebarEndOpen, and background. Desktop media is m = 1000 (min-width:1000px / max-width:999px). Nested p-button uses hide-label="true" and compact="true". Omit unset href. --p-animation-duration: 0s and --p-transition-duration: 0s. Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_canvas_before.png or /opt/cursor/artifacts/stencil_canvas_before.png (40157 bytes, SHA 28de5f4bfb3bfd7be7a26beebb89bb114b419f318af8c9e777e7dea52ff18c97). Do not recapture a new baseline. Own mitosis.config.js so prior lands are not regenerated. Strip @Component from canvas.tsx and full-restart --dev so bootstrapLazy drops p-canvas. If HTMLPCanvasElement disappears from components.d.ts, restore it via stub-side d.ts. Do not declare optional hidden on that interface. Recipe cssText/shadow/no-fragments/alias still apply. Probe Canvas.lite.tsx still uses tagName lit-canvas — land must emit p-canvas. After PNG path is /opt/cursor/artifacts/mitosis_land_canvas_after.png (not the probe mitosis_lit_canvas_after.png). Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles canvas as p-canvas. Mitosis Lit emits @customElement("p-canvas") with useShadowDom. rg my-fragment on generated Canvas.ts is empty. Live playground chrome crop pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true. Crop union of .header + sidebar headers. Do not include scrolling main.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
