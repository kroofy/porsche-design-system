GOAL         Land p-display from the Mitosis Lit probe into packages/components as the real p-display tag. Pixel-diff 0 vs the stored Stencil display playground baseline.
SCOPE        May write: packages/components (display source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/Display.lite.tsx if tagName must become p-display, .audit/orchestrate/stencil-to-mitosis/reports/land-display.md, /opt/cursor/artifacts/mitosis_land_display_after.png. May not write: other TAG_NAME sources, packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-text just passed: playground p-text is Mitosis Lit, baseline byte-equal (55035). Repeat that isolated wiring for p-display only. Playground: http://localhost:3333/?components=display. Card [data-card="display"]. Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_display_before.png or /opt/cursor/artifacts/stencil_display_before.png (30525 bytes). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from display.tsx and full-restart --dev so bootstrapLazy drops p-display. If the card is taller than the viewport, use page.screenshot({ clip }), not locator.screenshot() of a clipped descendant. Recipe cssText/shadow/no-fragments/alias still apply. Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles display as p-display. Mitosis Lit emits @customElement("p-display") with useShadowDom. rg my-fragment on generated Display.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
