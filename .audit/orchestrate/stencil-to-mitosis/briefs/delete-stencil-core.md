GOAL         Drop `@stencil/core` and `stencil build` from packages/components so Stencil is no longer a package dependency or production compiler. Prove playground divider + canvas pixel-diff stay 0.
SCOPE        May write: packages/components (package.json build/deps, stencil.config.ts, a local HTMLElement type shim replacing HTMLStencilElement imports), .audit/orchestrate/stencil-to-mitosis/reports/delete-stencil-core.md, verify artifacts under /opt/cursor/artifacts/delete_stencil_core_*.png. May not write: landed .lite.tsx sources, packages/mitosis-probe/**, wholesale stub/spec deletion, framework wrapper regeneration.
CONTEXT      delete-stencil just passed: `npm start` is serve-playground.mjs, not stencil --dev. Divider after is byte-equal (2641). Canvas after is byte-equal (40157, SHA 28de5f4bfb3bfd7be7a26beebb89bb114b419f318af8c9e777e7dea52ff18c97). Leftover: package.json `build` still runs `stencil build --docs`, `@stencil/core` 4.43.3 is a dependency, stencil.config.ts exists, stubs import HTMLStencilElement from @stencil/core/internal. Replace those type imports with a local shim (HTMLElement is enough). Remove the stencil build script and the dependency. Do not delete mitosis/ or IIFEs. Do not recapture baselines. Re-run divider + canvas pixel-diff vs stored baselines after the dep is gone. Viewport 1440x900 dsf 2. threshold 0 includeAA true. Start path must stay serve-playground.mjs. You are the only writer. Stay on cursor/mitosis-migration-41e6.
ACCEPTANCE   packages/components/package.json has no @stencil/core and no `stencil build` script. stencil.config.ts is gone or unused. rg @stencil/core on packages/components is empty (except historical comments). Live divider card and canvas chrome pixel-diff 0 vs stored baselines. Report written.
VERIFY       Same crops as delete-stencil. Divider + canvas vs stored baselines. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no re-landing a tag, no deleting IIFEs or mitosis output, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Prove playground still matches before claiming Stencil is gone.
