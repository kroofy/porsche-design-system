GOAL         Emit Mitosis framework output for p-divider to react, vue, angular, and svelte. Prove the four generators compile the existing Divider.lite.tsx. Do not break the already-landed Lit playground tag.
SCOPE        May write: packages/components/mitosis/divider (config, generated output under output/react|vue|angular|svelte, a generate script), .audit/orchestrate/stencil-to-mitosis/reports/generate-frameworks-divider.md. May not write: other TAG_NAME .lite.tsx, packages/mitosis-probe/**, wholesale overwrite of packages/components-react|vue|angular published wrappers, deleting Lit IIFEs, restoring Stencil.
CONTEXT      All 75 TAG_NAMES already serve Mitosis Lit. Stencil compiler and @stencil/core are gone from packages/components. The leftover wrapper pipeline (scripts/generateWrappers.ts) still assumes a Stencil dist/. Preferences originally named Mitosis targets customElement, react, vue, angular. customElement is dead. This unit is the lever for real Mitosis framework output: react, vue, angular, plus svelte. Source: packages/components/mitosis/divider/Divider.lite.tsx (tagName p-divider, cssText + useStyle). Keep the existing lit target and playground IIFE. Add targets in a way that does not regenerate/break Lit output — prefer a separate mitosis.config (e.g. mitosis.frameworks.config.js) and dest output/frameworks or per-target folders. mitosis build must exit 0. rg my-fragment on generated files must be empty. Do not call 760 m. m is 1000. You are the only writer. Stay on cursor/mitosis-migration-41e6.
ACCEPTANCE   mitosis build emits Divider output for react, vue, angular, and svelte. Each target has a generated file. rg my-fragment is empty on those files. Lit playground p-divider is untouched (do not rerun land-divider). Report lists output paths and any generator caveats. If a target cannot compile this lite file, write a dead-end for that target only and still ship the ones that work.
VERIFY       List generated files. mitosis build exit 0. my-fragment empty. Confirm packages/components/src/assets/p-divider.iife.js was not deleted.
TIMEBOX      45 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag, no customElement/webcomponent targets, no restoring stencil build.
REPORT       status, SHA, output paths per target, follow-ups.
STANDING     Lit stays the playground host. Framework outputs are generated artifacts, not a rewrite of the 75 lands.
