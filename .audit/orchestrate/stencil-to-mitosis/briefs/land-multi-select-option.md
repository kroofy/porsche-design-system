GOAL         Land p-multi-select-option from the Mitosis Lit probe into packages/components as the real p-multi-select-option tag. Pixel-diff 0 vs the stored Stencil multi-select-option playground baseline.
SCOPE        May write: packages/components (multi-select-option source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/MultiSelectOption.lite.tsx if tagName must become p-multi-select-option, .audit/orchestrate/stencil-to-mitosis/reports/land-multi-select-option.md, /opt/cursor/artifacts/mitosis_land_multi_select_option_after.png. May not write: other TAG_NAME sources (including the already-landed parent), packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-multi-select just passed: playground p-multi-select is Mitosis Lit, baseline byte-equal (33197). Repeat that isolated wiring for p-multi-select-option only. Playground: http://localhost:3333/?components=multi-select. There is no separate option card — crop [data-card="multi-select"] (4 closed parent hosts, 20 options). Compare the closed card. Parent is already Mitosis (LitMultiSelect). Optgroups are already Mitosis (LitOptgroup). Copy light-DOM children. throwIfParentIsNotOfKind on in-parent swap is benign. Options sit in a display:none popover — wait attached, not visible. Closed card selected-text stays empty (no value). --p-animation-duration: 0s and --p-transition-duration: 0s. Do not declare optional hidden on HTMLPMultiSelectOptionElement if that breaks native HTMLElement.hidden (see land-select-option host-type fix). Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_multi_select_option_before.png or /opt/cursor/artifacts/stencil_multi_select_option_before.png (33197 bytes, same crop as the parent card). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from multi-select-option.tsx and full-restart --dev so bootstrapLazy drops p-multi-select-option. If HTMLPMultiSelectOptionElement disappears from components.d.ts, restore it via stub-side d.ts. Recipe cssText/shadow/no-fragments/alias still apply. Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles multi-select-option as p-multi-select-option. Mitosis Lit emits @customElement("p-multi-select-option") with useShadowDom. rg my-fragment on generated MultiSelectOption.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
