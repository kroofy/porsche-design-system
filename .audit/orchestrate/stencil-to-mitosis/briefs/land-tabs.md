GOAL         Land p-tabs from the Mitosis Lit probe into packages/components as the real p-tabs tag. Pixel-diff 0 vs the stored Stencil tabs playground baseline.
SCOPE        May write: packages/components (tabs source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/Tabs.lite.tsx if tagName must become p-tabs, .audit/orchestrate/stencil-to-mitosis/reports/land-tabs.md, /opt/cursor/artifacts/mitosis_land_tabs_after.png. May not write: other TAG_NAME sources (including tabs-item), packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-tabs-bar just passed: playground p-tabs-bar is Mitosis Lit, baseline byte-equal (85161). Repeat that isolated wiring for p-tabs only. Playground: http://localhost:3333/?components=tabs. Card [data-card="tabs"] spans 2 columns — 5 hosts (default, canvas, surface, frosted, size=medium), each with three p-tabs-item and nested p-text. display:contents wrap around nested p-tabs-bar.root (already Mitosis) plus default slot of p-tabs-item. Items stay Stencil this unit. Nested p-text already Mitosis. Copy light-DOM children. throwIfParentIsNotOfKind on item reconnect is benign. Rebuild bar buttons from item labels and forward size / background / compact / activeTabIndex (default 0). --p-animation-duration: 0s, --p-transition-duration: 0s, --p-duration-md: 0s, --p-duration-sm: 0s. size m is 1000. Card may be taller than 900; clip to viewport remainder with page.screenshot({ clip }). Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_tabs_before.png or /opt/cursor/artifacts/stencil_tabs_before.png (91188 bytes). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from tabs.tsx and full-restart --dev so bootstrapLazy drops p-tabs. If HTMLPTabsElement disappears from components.d.ts, restore it via stub-side d.ts. Recipe cssText/shadow/no-fragments/alias still apply. Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles tabs as p-tabs. Mitosis Lit emits @customElement("p-tabs") with useShadowDom. rg my-fragment on generated Tabs.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
