GOAL         Land p-input-url from the Mitosis Lit probe into packages/components as the real p-input-url tag. Pixel-diff 0 vs the stored Stencil input-url playground baseline.
SCOPE        May write: packages/components (input-url source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/InputUrl.lite.tsx if tagName must become p-input-url, .audit/orchestrate/stencil-to-mitosis/reports/land-input-url.md, /opt/cursor/artifacts/mitosis_land_input_url_after.png. May not write: other TAG_NAME sources, packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-input-search just passed: playground p-input-search is Mitosis Lit, baseline byte-equal (63484). Repeat that isolated wiring for p-input-url only. Playground: http://localhost:3333/?components=input-url. Card [data-card="input-url"] — 6 hosts, all indicator (default, loading, success+message, error+message, disabled, read-only). Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_input_url_before.png or /opt/cursor/artifacts/stencil_input_url_before.png (63042 bytes). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from input-url.tsx and full-restart --dev so bootstrapLazy drops p-input-url. Nested icons/spinners stay <p-icon>/<p-spinner> (already Mitosis). If HTMLPInputUrlElement disappears from components.d.ts, restore it — search land kept HTMLPInputSearchElement via a stub-side d.ts because Stencil --dev regenerates components.d.ts. Tall card: page.screenshot({ clip }). Recipe cssText/shadow/no-fragments/alias still apply. Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles input-url as p-input-url. Mitosis Lit emits @customElement("p-input-url") with useShadowDom. rg my-fragment on generated InputUrl.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
