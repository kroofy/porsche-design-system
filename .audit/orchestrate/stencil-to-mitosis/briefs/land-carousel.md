GOAL         Land p-carousel from the Mitosis Lit probe into packages/components as the real p-carousel tag. Pixel-diff 0 vs the stored Stencil carousel playground baseline.
SCOPE        May write: packages/components (carousel source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/Carousel.lite.tsx if tagName must become p-carousel, .audit/orchestrate/stencil-to-mitosis/reports/land-carousel.md, /opt/cursor/artifacts/mitosis_land_carousel_after.png. May not write: other TAG_NAME sources (including canvas), packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-drilldown-link just passed: playground p-drilldown-link is Mitosis Lit, closed-card baseline byte-equal (18500). Repeat that isolated wiring for p-carousel only. Playground: http://localhost:3333/?components=carousel. Card [data-card="carousel"] is a closed native popover with a Show Carousel button. Five p-carousel hosts sit inside #popover-carousel. Compare the default closed card — do not open the popover. Crop [data-card="carousel"] (460×220 at dsf 2). Nested p-heading / p-scroller / p-tag / p-text / p-button-pure (prev/next) are already-landed Mitosis. Copy light-DOM children and named heading / description / controls slots. :host is display:flex (column). Markup includes .header + #splide.splide > .splide__track > .splide__list > slot + .slide-status. Strip the dummy Mitosis .root. Probe Splide must use speed: 0 (Stencil speed is carouselTransitionDuration * 1000). Pause motion with --p-animation-duration: 0s, --p-transition-duration: 0s, --p-duration-md: 0s, --p-duration-sm: 0s. Wait { state: 'attached' } for hosts inside the closed popover. Breakpoint padding uses s = 760 and xxl = 1920. Do not call 760 m. m is 1000. Width basic multiplies the grid column term by 2; extended by 1. Parse JSON props (slides-per-page / pagination / intl) including unquoted keys. Omit unset href so it does not become "undefined". Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_carousel_before.png or /opt/cursor/artifacts/stencil_carousel_before.png (7096 bytes, SHA 61bd4864ddbae06db394c375ac4320c812a0869863caf1402fe415666cc12b84). Do not recapture a new baseline. Own mitosis.config.js so prior lands are not regenerated. Strip @Component from carousel.tsx and full-restart --dev so bootstrapLazy drops p-carousel. If HTMLPCarouselElement disappears from components.d.ts, restore it via stub-side d.ts. Do not declare optional hidden on that interface. Recipe cssText/shadow/no-fragments/alias still apply. Probe Carousel.lite.tsx still uses tagName lit-carousel — land must emit p-carousel. After PNG path is /opt/cursor/artifacts/mitosis_land_carousel_after.png (not the probe mitosis_lit_carousel_after.png). Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles carousel as p-carousel. Mitosis Lit emits @customElement("p-carousel") with useShadowDom. rg my-fragment on generated Carousel.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true. Crop closed [data-card="carousel"]. Do not open the popover.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
