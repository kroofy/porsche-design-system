GOAL         Land p-text-list from the Mitosis Lit probe into packages/components as the real p-text-list tag. Pixel-diff 0 vs the stored Stencil text-list playground baseline.
SCOPE        May write: packages/components (text-list source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/TextList.lite.tsx if tagName must become p-text-list, .audit/orchestrate/stencil-to-mitosis/reports/land-text-list.md, /opt/cursor/artifacts/mitosis_land_text_list_after.png. May not write: other TAG_NAME sources, packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-fieldset just passed: playground p-fieldset is Mitosis Lit, baseline byte-equal (57315). Repeat that isolated wiring for p-text-list only. Playground: http://localhost:3333/?components=text-list. Card [data-card="text-list"] — 3 hosts (unordered, numbered, alphabetically), each with nested p-text-list-item and a nested p-text-list. Items stay Stencil. Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_text_list_before.png or /opt/cursor/artifacts/stencil_text_list_before.png (31087 bytes). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from text-list.tsx and full-restart --dev so bootstrapLazy drops p-text-list. Copy light-DOM children + nested lists on swap. If HTMLPTextListElement disappears from components.d.ts, restore it via stub-side d.ts like land-input-search. Tall card: page.screenshot({ clip }). Recipe cssText/shadow/no-fragments/alias still apply. Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles text-list as p-text-list. Mitosis Lit emits @customElement("p-text-list") with useShadowDom. rg my-fragment on generated TextList.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
