GOAL         Land p-segmented-control-item from the Mitosis Lit probe into packages/components as the real p-segmented-control-item tag. Pixel-diff 0 vs the stored Stencil segmented-control-item playground baseline.
SCOPE        May write: packages/components (segmented-control-item source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/SegmentedControlItem.lite.tsx if tagName must become p-segmented-control-item, .audit/orchestrate/stencil-to-mitosis/reports/land-segmented-control-item.md, /opt/cursor/artifacts/mitosis_land_segmented_control_item_after.png. May not write: other TAG_NAME sources (including the already-landed parent), packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-segmented-control just passed: playground p-segmented-control is Mitosis Lit, baseline byte-equal (36882). Repeat that isolated wiring for p-segmented-control-item only. Playground: http://localhost:3333/?components=segmented-control. There is no separate item card — crop [data-card="segmented-control"] (3 parent hosts, all class=w-full, four items each: default, disabled=true, mixed item-disabled). Parent is already Mitosis (LitSegmentedControl) and stamps selected / compact / state / message / disabledParent plus like iconSource to http://localhost:3001/icons/like.a7468cd.svg (LitIcon only maps car and arrow-right). Nested p-icon stays Mitosis. Copy light-DOM children. throwIfParentIsNotOfKind on in-parent swap is benign. Stencil item uses delegatesFocus; Mitosis Lit does not emit it. Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_segmented_control_item_before.png or /opt/cursor/artifacts/stencil_segmented_control_item_before.png (36882 bytes, same crop as the parent card). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from segmented-control-item.tsx and full-restart --dev so bootstrapLazy drops p-segmented-control-item. If HTMLPSegmentedControlItemElement disappears from components.d.ts, restore it via stub-side d.ts. Recipe cssText/shadow/no-fragments/alias still apply. Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles segmented-control-item as p-segmented-control-item. Mitosis Lit emits @customElement("p-segmented-control-item") with useShadowDom. rg my-fragment on generated SegmentedControlItem.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
