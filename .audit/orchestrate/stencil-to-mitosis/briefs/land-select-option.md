GOAL         Land p-select-option from the Mitosis Lit probe into packages/components as the real p-select-option tag. Pixel-diff 0 vs the stored Stencil select-option playground baseline.
SCOPE        May write: packages/components (select-option source, stencil exclude, mitosis build wiring for this tag only), packages/mitosis-probe-lit/src/SelectOption.lite.tsx if tagName must become p-select-option, .audit/orchestrate/stencil-to-mitosis/reports/land-select-option.md, /opt/cursor/artifacts/mitosis_land_select_option_after.png. May not write: other TAG_NAME sources (including the already-landed parent and optgroup), packages/mitosis-probe/**, wholesale Stencil deletion, framework wrappers.
CONTEXT      land-select just passed: playground p-select is Mitosis Lit, baseline byte-equal (33197). Repeat that isolated wiring for p-select-option only. Playground: http://localhost:3333/?components=select. There is no separate option card — crop [data-card="select"] (4 closed parent hosts, 20 options). Compare the closed card. Parent is already Mitosis (LitSelect). Optgroups stay Stencil. Nested p-icon stay Mitosis. Copy light-DOM children. throwIfParentIsNotOfKind on in-parent swap is benign. Options sit in a display:none popover — wait attached, not visible. Closed card selected-text stays empty (no value). --p-animation-duration: 0s and --p-transition-duration: 0s. Stored baseline: .audit/orchestrate/stencil-to-mitosis/baseline/stencil_select_option_before.png or /opt/cursor/artifacts/stencil_select_option_before.png (33197 bytes, same crop as the parent card). Own mitosis.config.js so prior lands are not regenerated. Strip @Component from select-option.tsx and full-restart --dev so bootstrapLazy drops p-select-option. If HTMLPSelectOptionElement disappears from components.d.ts, restore it via stub-side d.ts. Recipe cssText/shadow/no-fragments/alias still apply. Do not edit the baseline PNG. You are the only writer.
ACCEPTANCE   Stencil no longer compiles select-option as p-select-option. Mitosis Lit emits @customElement("p-select-option") with useShadowDom. rg my-fragment on generated SelectOption.ts is empty. Live playground card pixel-diff 0 vs stored baseline. Report written.
VERIFY       Recipe pixel-diff. Viewport 1440x900 dsf 2. threshold 0 includeAA true.
TIMEBOX      60 minutes.
FORBIDDEN    no gt, no rebase, no force-push, no second tag land, no deleting the Stencil toolchain, no customElement/webcomponent targets.
REPORT       status, SHA, pixel-diff counts, follow-ups.
STANDING     Target is lit. m is 1000px. cssText in shadow. No fragments. Alias lit/decorators.js. One TAG_NAME only.
